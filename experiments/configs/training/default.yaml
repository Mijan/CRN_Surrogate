lr: 1.0e-3
max_epochs: 200
batch_size: 16
n_ssa_samples: 32
dt: 0.1
grad_clip_norm: 1.0
scheduler_type: cosine
val_every: 10
checkpoint_every: 0
num_workers: 0
shuffle_train: true
gpu_memory_fraction: 0.5
